---
description: Cut a release (verify, branch, merge, tag, deploy check).
---

Run the `release` skill for version: $ARGUMENTS

If no version is given, suggest the next semver and confirm before proceeding.
