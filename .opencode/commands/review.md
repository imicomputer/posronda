---
description: Review a pull request (diff, tests, verdict).
---

Run the `code-review` skill for: $ARGUMENTS

If no PR number is given, list open PRs and ask which one. Post the verdict on the PR with `gh` and report back.
