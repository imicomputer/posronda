---
description: Build a feature end-to-end (analyze, design, branch, code, test, PR).
---

Run the `feature-dev` skill pipeline for this request: $ARGUMENTS

Follow every phase — analyze, design, branch, code, test (`npm test` + `npm run build`), commit, push, pull request, then self-review with the `code-review` skill. Report the PR URL at the end.
